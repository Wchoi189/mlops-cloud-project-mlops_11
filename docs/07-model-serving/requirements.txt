# 7단계: ML 프레임워크와 모델 서빙 통합 - 필수 패키지
# MLOps Stage 7: ML Framework and Model Serving Integration
# 다양한 ML 프레임워크와 고성능 모델 서빙을 위한 핵심 도구들

# =============================================================================
# FastAPI 및 웹 프레임워크
# =============================================================================
fastapi>=0.115.6,<1.0.0                    # FastAPI 프레임워크
uvicorn[standard]>=0.32.1,<1.0.0           # ASGI 서버
pydantic>=2.10.3,<3.0.0                    # 데이터 검증 및 직렬화

# =============================================================================
# MLflow 모델 서빙
# =============================================================================
mlflow>=2.22.0,<3.0.0                      # 모델 로딩 및 서빙
mlflow-skinny>=2.22.0,<3.0.0               # MLflow 경량 버전

# =============================================================================
# 기본 ML 프레임워크
# =============================================================================
scikit-learn>=1.6.1,<2.0.0                 # 기본 ML 알고리즘
numpy>=1.26.4,<2.0.0                       # 수치 계산
pandas>=2.2.3,<3.0.0                       # 데이터 처리

# =============================================================================
# 캐싱 시스템
# =============================================================================
redis>=5.2.1,<6.0.0                        # Redis 클라이언트
hiredis>=3.0.0,<4.0.0                      # Redis 고성능 파서

# =============================================================================
# 비동기 처리
# =============================================================================
asyncio-mqtt>=0.16.2,<1.0.0                # 비동기 MQTT (선택사항)
aiofiles>=24.1.0,<25.0.0                   # 비동기 파일 I/O

# =============================================================================
# HTTP 클라이언트
# =============================================================================
httpx>=0.27.2,<1.0.0                       # 비동기 HTTP 클라이언트
requests>=2.32.3,<3.0.0                    # 동기 HTTP 클라이언트

# =============================================================================
# 환경 설정
# =============================================================================
python-dotenv>=1.0.1,<2.0.0               # 환경 변수 관리
pyyaml>=6.0.2,<7.0.0                       # YAML 설정

# =============================================================================
# 로깅 및 모니터링
# =============================================================================
structlog>=24.5.0,<25.0.0                  # 구조화된 로깅
prometheus-client>=0.21.1,<1.0.0           # Prometheus 메트릭

# =============================================================================
# 데이터 직렬화
# =============================================================================
orjson>=3.10.12,<4.0.0                     # 고성능 JSON 직렬화
msgpack>=1.1.0,<2.0.0                      # MessagePack 직렬화

# =============================================================================
# 시간 및 유틸리티
# =============================================================================
python-dateutil>=2.9.0,<3.0.0             # 날짜/시간 처리
