# Minimal MLOps Requirements - Fast Installation
# Core libraries only - reduced from 50+ to 15 essential packages

# ===========================================
# CORE DATA SCIENCE (Essential)
# ===========================================
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0

# ===========================================
# API FRAMEWORK (Essential)
# ===========================================
fastapi>=0.85.0
uvicorn>=0.18.0

# ===========================================
# MLOPS CORE (Essential)
# ===========================================
mlflow>=2.0.0

# ===========================================
# DATA VALIDATION (Essential)
# ===========================================
pydantic>=1.10.0

# ===========================================
# UTILITIES (Essential)
# ===========================================
requests>=2.28.0
python-dotenv>=1.0.0

# ===========================================
# MACHINE LEARNING SPECIFIC (Lightweight)
# ===========================================
joblib>=1.2.0

# ===========================================
# OPTIONAL ENHANCEMENTS (Install separately if needed)
# ===========================================
# Uncomment only if you need these features:

# For better debugging (optional)
icecream>=2.1.3

# For progress bars (optional) 
tqdm>=4.66.0

# For beautiful terminal output (optional)
rich>=13.7.0

# For CLI interfaces (optional)
fire>=0.5.0

# For plotting (optional - install only if doing visualization)
# matplotlib>=3.5.0
# seaborn>=0.11.0

# ===========================================
# REMOVED HEAVY PACKAGES (install separately if needed):
# ===========================================
# torch - Very heavy (500MB+), install separately: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# transformers - Heavy (200MB+), not needed for basic MLOps
# datasets - Heavy, not needed for basic functionality  
# tensorboard - Heavy, MLflow provides similar functionality
# apache-airflow - Very heavy (300MB+), not needed for basic pipeline
# prefect - Heavy workflow engine, not needed initially
# nltk - NLP library, not needed for basic movie rating prediction
# evidently - Heavy ML monitoring, can add later
# opencv-python - Computer vision, not needed
# pillow - Image processing, not needed
# plotly - Heavy plotting library, matplotlib is sufficient
# boto3 - AWS SDK, only needed for cloud deployment
# cryptography - Heavy security library, not needed initially